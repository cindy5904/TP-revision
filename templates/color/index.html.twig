{% extends 'base.html.twig' %}

{% block title %}Hello ColorController!{% endblock %}

{% block body %}
    <div>
        <h1  class="mt-8 text-6xl font-bold text-center" style="color:{{color}}">{{ color }}</h1>
        {% if message is defined %}
            <h2 class="mt-8 text-6xl font-bold text-center" style="color:{{ color }}">{{ message }}</h2>
            <img class="w-1/2 max-w-full h-auto mt-8 mx-auto" src="{{asset('assets/images/attention.gif')}}" alt="Attention gif">

        {% endif %}
    </div>
    <h4 class="font-bold text-center">Un petit commit sur une nouvelle branche car j'ai oublié de changer de branche sur cet exercice</h4>
    <p class="text-center text-pink-400">Mince Alors ! &#x1F61C</p>
{% endblock %}
